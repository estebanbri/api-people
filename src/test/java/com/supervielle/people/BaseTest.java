package com.supervielle.people;

import com.supervielle.people.config.Config;
import org.springframework.test.context.ContextConfiguration;

@ContextConfiguration(classes = Config.class)
public class BaseTest {

}
